if data.raw["double-setting"]["data-dd-dark-perc"] then
	data.raw["double-setting"]["data-dd-dark-perc"].default_value = 70 -- 10% less than mod default, it's more playable
end

if data.raw["bool-setting"]["data-dd-enable-flares"] then
	data.raw["bool-setting"]["data-dd-enable-flares"].default_value = true
end
